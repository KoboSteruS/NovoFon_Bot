# 🔧 Разблокировка apt/dpkg на сервере

## Быстрое решение

Выполни эти команды **на сервере** по порядку:

```bash
# 1. Убиваем зависшие процессы apt
sudo pkill -9 apt
sudo pkill -9 dpkg

# 2. Удаляем блокировочные файлы
sudo rm -f /var/lib/dpkg/lock-frontend
sudo rm -f /var/lib/dpkg/lock
sudo rm -f /var/cache/apt/archives/lock
sudo rm -f /var/lib/apt/lists/lock

# 3. Восстанавливаем состояние dpkg
sudo dpkg --configure -a

# 4. Проверяем apt
sudo apt-get update

# 5. Если всё ок, продолжаем установку
sudo bash install.sh
```

## Если не помогло

```bash
# Проверяем, что держит блокировку
sudo lsof /var/lib/dpkg/lock-frontend

# Если видишь процесс - убиваем его по PID
sudo kill -9 <PID>

# Или более жёсткий вариант - перезагрузка
sudo reboot
```

## Альтернатива: использовать скрипт

Если скрипт `fix_apt_lock.sh` уже на сервере:

```bash
sudo bash fix_apt_lock.sh
```

