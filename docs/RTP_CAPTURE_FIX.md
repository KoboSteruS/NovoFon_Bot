# Исправление проблемы с захватом RTP аудио

## Проблема

Бот не получает события `ChannelMediaReceived` от snoop channel, поэтому не слышит пользователя и не может вести непрерывный диалог.

## Диагностика

### Что видно в логах:

✅ **Работает:**
- StasisStart → Answer → Snoop создан
- TTS → SLIN16 → Playback успешно
- PlaybackFinished

❌ **НЕ работает:**
- НЕТ событий `ChannelMediaReceived`
- Бот не получает RTP аудио от пользователя

## Возможные причины

### 1. `canreinvite=yes` в sip.conf (chan_sip)

**Проблема:** Если `canreinvite=yes`, RTP идет напрямую между провайдером и клиентом, минуя Asterisk.

**Решение:** Убедитесь, что в `/etc/asterisk/sip.conf` для вашего провайдера:

```ini
[606147]  ; или [novofon-ip]
type=peer
canreinvite=no  ; ← ВАЖНО: должно быть NO
```

**Проверка:**
```bash
grep -A 20 "\[606147\]" /etc/asterisk/sip.conf | grep canreinvite
# Должно быть: canreinvite=no
```

### 2. `direct_media=yes` в pjsip.conf (pjsip)

**Проблема:** Если используется `pjsip` и `direct_media=yes`, RTP идет напрямую.

**Решение:** Убедитесь, что в `/etc/asterisk/pjsip.conf`:

```ini
[novofon]
type=endpoint
direct_media=no  ; ← ВАЖНО: должно быть NO
```

**Проверка:**
```bash
grep -A 10 "\[novofon\]" /etc/asterisk/pjsip.conf | grep direct_media
# Должно быть: direct_media=no
```

### 3. Snoop создан с неправильными параметрами

**Проблема:** Если `spy="in"` вместо `spy="both"`, бот не будет слышать входящий RTP.

**Решение:** В коде уже установлено `spy="both"`, но проверьте логи:

```bash
grep "Snoop configuration" logs/app.log
# Должно быть: spy=both, whisper=none
```

### 4. Asterisk не отправляет ChannelMediaReceived автоматически

**Проблема:** В некоторых версиях Asterisk snoop channels не отправляют `ChannelMediaReceived` события автоматически.

**Решение:** Нужно использовать альтернативный подход (см. ниже).

## Решения

### Решение 1: Проверить и исправить canreinvite/direct_media

1. **Для chan_sip (sip.conf):**
```bash
# Проверьте текущую настройку
asterisk -rx "sip show peer 606147" | grep canreinvite

# Если canreinvite=yes, исправьте в sip.conf:
# canreinvite=no
```

2. **Для pjsip (pjsip.conf):**
```bash
# Проверьте текущую настройку
asterisk -rx "pjsip show endpoint novofon" | grep direct_media

# Если direct_media=yes, исправьте в pjsip.conf:
# direct_media=no
```

3. **Перезагрузите конфигурацию:**
```bash
asterisk -rx "sip reload"  # для chan_sip
# или
asterisk -rx "module reload res_pjsip.so"  # для pjsip
```

### Решение 2: Использовать externalMedia вместо snoop

Если snoop не работает, можно использовать `externalMedia`:

```python
# Создать локальный RTP сервер для приема аудио
# И использовать externalMedia для отправки аудио в этот сервер
```

Но это требует создания RTP сервера на Python.

### Решение 3: Использовать запись канала (recording)

Можно записывать канал и обрабатывать записанные файлы:

```python
# Записать канал
recording = await self.ari.start_recording(
    channel_id=channel_id,
    name=f"call_{channel_id}",
    format="wav"
)

# Обрабатывать записанные файлы
# (но это не real-time)
```

### Решение 4: Использовать MixMonitor в dialplan

Добавить `MixMonitor()` в dialplan для принудительного захвата RTP:

```ini
# /etc/asterisk/extensions.conf
[from-novofon]
exten => s,1,Stasis(novofon_bot)
exten => s,n,MixMonitor(call-${UNIQUEID}.wav)  ; ← Запись для диагностики
exten => s,n,Hangup()
```

Но это не решит проблему с real-time обработкой.

## Проверка после исправления

1. **Позвоните боту**
2. **Проверьте логи:**
```bash
journalctl -u novofon-bot -f | grep -E "ChannelMediaReceived|Media received|Sent.*bytes of.*audio"
```

3. **Должны появиться:**
```
🔊 ChannelMediaReceived event received!
Event data: {...}
✅ Sent X bytes of pcmu audio to processor
```

4. **Если события появились:**
   - Бот должен начать слышать пользователя
   - ASR должен возвращать транскрипты
   - Бот должен отвечать на реплики пользователя

## Текущий статус кода

✅ **Уже исправлено:**
- `spy="both"` в snoop_channel()
- Добавлено подробное логирование в `_on_media_received()`
- Добавлено логирование конфигурации snoop

❓ **Нужно проверить на сервере:**
- `canreinvite=no` в sip.conf (для chan_sip)
- `direct_media=no` в pjsip.conf (для pjsip)
- Версия Asterisk и поддержка ChannelMediaReceived для snoop

## Рекомендации

1. **Сначала проверьте canreinvite/direct_media** - это самая частая причина
2. **Если не помогло** - проверьте версию Asterisk и документацию по snoop channels
3. **Если все равно не работает** - рассмотрите альтернативные подходы (externalMedia, recording)




