# 🔑 Как получить SIP логин и пароль от NovoFon

## Проблема

NovoFon требует верификацию IP (тестовый звонок), но для звонка нужны логин и пароль, которые дают только после верификации.

## Решения

### Вариант 1: Связаться с поддержкой NovoFon (рекомендуется)

**Что сказать:**

> "Здравствуйте! Я создал SIP-транк в личном кабинете и указал адреса терминации (109.73.192.126). Система требует совершить тестовый звонок для верификации IP, но для звонка нужны SIP логин и пароль, которых у меня пока нет. Можете предоставить временные данные для верификации или активировать транк вручную?"

**Куда обращаться:**
- Онлайн-чат в личном кабинете
- Email: support@novofon.ru
- Телефон поддержки (на сайте novofon.ru)

---

### Вариант 2: Проверить другие разделы личного кабинета

#### 2.1. Раздел "API" или "Интеграции"
- Могут быть общие SIP данные для всех транков
- Или отдельные ключи доступа

#### 2.2. Настройки номера
- Зайди в раздел с твоими номерами
- Выбери номер (+79581114585)
- Ищи "SIP-доступ", "Пароли", "API ключи"

#### 2.3. Безопасность/Доступ
- Раздел "Безопасность"
- "Пароли для SIP"
- "API пароли"

#### 2.4. Email от NovoFon
- Проверь почту при регистрации/подключении
- Там могли прислать SIP данные

---

### Вариант 3: Временная настройка без верификации

Если NovoFon позволяет создать транк без верификации:

1. **Создай транк** с адресами терминации
2. **Проверь, появились ли логин/пароль** на странице транка
3. Если нет - используй вариант 1 (поддержка)

---

### Вариант 4: Использовать другой способ верификации

Спроси у поддержки:
- Можно ли верифицировать IP другим способом?
- Можно ли получить временные данные для теста?
- Можно ли активировать транк вручную после проверки IP?

---

## Что делать после получения данных

### 1. Обнови pjsip.conf

```bash
sudo nano /etc/asterisk/pjsip.conf
```

Найди секцию:
```ini
[novofon]
type = auth
auth_type = userpass
username = YOUR_SIP_LOGIN_HERE
password = YOUR_SIP_PASSWORD_HERE
```

Замени на реальные данные:
```ini
[novofon]
type = auth
auth_type = userpass
username = ТВОЙ_ЛОГИН
password = ТВОЙ_ПАРОЛЬ
```

### 2. Перезагрузи PJSIP

```bash
sudo asterisk -rx "pjsip reload"
```

### 3. Сделай тестовый звонок

```bash
sudo asterisk -rvvv
```

В консоли:
```bash
channel originate Local/79991234567@outgoing application Playback hello-world
```

### 4. Проверь верификацию

- Вернись в личный кабинет NovoFon
- Проверь статус транка - должен стать активным
- Получи постоянные логин/пароль (если были временные)

---

## Альтернатива: Использовать NovoFon API

Если SIP-транк не получается настроить, можно использовать NovoFon API для исходящих звонков (но это менее гибко, чем SIP).

---

## Резюме

**Самый быстрый способ:** Связаться с поддержкой NovoFon и объяснить ситуацию. Они обычно помогают в таких случаях.

**Что иметь под рукой:**
- Номер транка (если есть)
- IP адрес сервера (109.73.192.126)
- Твой номер телефона (+79581114585)

