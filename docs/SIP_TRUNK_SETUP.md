# 📞 Настройка SIP-транка в NovoFon

## Что делать на странице "Настройка SIP-транка"

### 1. Заполни обязательные поля:

#### Адреса терминации (обязательно, красная рамка):
- **Первое поле:** `109.73.192.126` (твой публичный IP)
- **Второе поле:** `109.73.192.126` (тот же IP, если один сервер)
- Или нажми **"Добавить"** если нужно несколько IP

#### Другие настройки:
- **Имя SIP-транка:** Можешь оставить пустым или написать `asterisk` или `novofon_bot`
- **Канальность:** Оставь 5 (или сколько нужно одновременных звонков)
- **Разрешенная связь:** Оставь "Входящие, Исходящие" ✅
- **Статус:** Оставь "Активен" ✅

### 2. Нажми "Создать"

После создания транка:
- NovoFon покажет тебе **логин** и **пароль** для этого транка
- **Запиши их!** Они понадобятся для настройки Asterisk

---

## Где найти логин и пароль после создания

После создания SIP-транка:

### Вариант 1: На странице транка
1. После создания откроется страница с деталями транка
2. Там должны быть поля:
   - **Логин** (username)
   - **Пароль** (password)

### Вариант 2: Список транков
1. Вернись в список SIP-транков
2. Нажми на созданный транк
3. Там будут логин и пароль

### Вариант 3: Email/Уведомления
- NovoFon может отправить логин и пароль на email
- Проверь почту

---

## Что делать дальше

После получения логина и пароля:

### 1. Запусти скрипт настройки Asterisk:

```bash
cd ~/NovoFon_Bot
sudo bash setup_asterisk_calls.sh
```

### 2. Введи данные:
- **SIP логин:** тот, что дал NovoFon
- **SIP пароль:** тот, что дал NovoFon
- **SIP сервер:** `sip.novofon.ru:5060` (уже знаешь)
- **Caller ID:** твой номер (+79581114585)
- **Публичный IP:** `109.73.192.126`

### 3. Проверь работу:

```bash
# Проверить регистрацию
sudo asterisk -rx "pjsip show endpoints"

# Проверить логи
sudo journalctl -u novofon-bot -f
```

---

## Если не нашёл логин/пароль

1. **Создай транк** с адресами терминации (109.73.192.126)
2. **Свяжись с поддержкой** NovoFon и скажи:
   > "Я создал SIP-транк, но не вижу логин и пароль для подключения Asterisk. Можете прислать?"

3. Или **проверь email** - туда могли отправить данные

---

## Важно!

- **Адреса терминации** - это IP твоего сервера, откуда будут приниматься звонки
- Если у тебя один сервер - заполни оба поля одинаковым IP
- Если несколько серверов - добавь все через кнопку "Добавить"
- **Логин и пароль** обычно показываются **после создания** транка

