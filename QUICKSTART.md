# 🚀 Быстрый старт - NovoFon Bot

## ⚡ Экспресс-тестирование (5 минут)

### 1. Получите API ключ NovoFon

https://novofon.com/ → Личный кабинет → Пользователи АТС → API

### 2. Создайте `.env` файл

```bash
# Создайте файл .env в корне проекта
NOVOFON_API_KEY=ваш_ключ_здесь
NOVOFON_FROM_NUMBER=+79991234567
NOVOFON_API_URL=https://api.novofon.com

# Minimal config (БД пока не нужна для теста)
APP_ENV=development
DEBUG=true
```

### 3. Установите зависимости

```bash
# Создайте venv
python -m venv venv

# Активируйте (Windows)
venv\Scripts\activate

# Установите
pip install httpx python-dotenv
```

### 4. Проверьте NovoFon API

```bash
# Быстрая проверка (без БД)
python test_novofon_simple.py
```

Если видите ✅ - значит API работает!

---

## 📦 Полная установка (с БД)

### 1. Настройте окружение

```bash
# Интерактивная настройка
python setup_env.py
```

### 2. Установите все зависимости

```bash
pip install -r requirements.txt
```

### 3. Настройте PostgreSQL

```bash
# Создайте базу данных
createdb novofon_bot

# Или через psql
psql -U postgres -c "CREATE DATABASE novofon_bot;"
```

### 4. Запустите сервер

```bash
python run_dev.py
```

### 5. Откройте Swagger UI

```
http://localhost:8000/docs
```

### 6. Протестируйте звонок

В Swagger UI:
1. Найдите `POST /api/calls/initiate`
2. Нажмите "Try it out"
3. Введите:
   ```json
   {
     "phone": "+79991234567"
   }
   ```
4. Нажмите "Execute"

---

## 🧪 Тестирование через curl

```bash
# Health check
curl http://localhost:8000/health

# Инициировать звонок
curl -X POST "http://localhost:8000/api/calls/initiate" \
  -H "Content-Type: application/json" \
  -d '{"phone": "+79991234567"}'

# Получить список звонков
curl http://localhost:8000/api/calls
```

---

## 🆘 Проблемы?

### NovoFon API недоступен
- Проверьте API ключ в `.env`
- Проверьте интернет соединение
- Логи: `logs/app.log`

### База данных не подключается
- Проверьте, что PostgreSQL запущен
- Проверьте `DATABASE_URL` в `.env`
- Для первого теста можно обойтись без БД (используйте `test_novofon_simple.py`)

### Импорты не работают
- Активируйте venv: `venv\Scripts\activate`
- Установите зависимости: `pip install -r requirements.txt`

---

## 📚 Дополнительная документация

- **NovoFon API**: `docs/NOVOFON_API_TESTING.md`
- **Полное README**: `README.md`
- **Swagger**: `http://localhost:8000/docs` (после запуска)

---

## ✅ Чек-лист готовности

- [ ] Получил API ключ NovoFon
- [ ] Создал `.env` файл
- [ ] Установил зависимости
- [ ] Запустил `test_novofon_simple.py` - ✅
- [ ] Настроил PostgreSQL (опционально)
- [ ] Запустил сервер `python run_dev.py`
- [ ] Открыл `http://localhost:8000/docs`
- [ ] Сделал тестовый звонок

🎉 **Готово!**

