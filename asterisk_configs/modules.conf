;
; Asterisk Modules Configuration
; Загрузка модулей для WebSocket поддержки
;

; WebSocket modules для PJSIP
load => res_http_websocket.so
load => res_pjsip_transport_websocket.so

; Остальные модули загружаются автоматически или через autoload.conf
; Если используете autoload.conf, добавьте эти строки туда



